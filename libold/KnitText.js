(function() {
    
    Knit.VectorGlyph = function(contour, holes, xadvance, lineHeight) {
        this.contour = contour || [];
        this.holes = holes || [];
        this.xadvance = xadvance || 0;
        this.lineHeight = lineHeight || 0;
    };
    
    Knit.createVectorGlyph = function(
    
})();